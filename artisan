#!/usr/bin/env php
<?php

/**
 * MOTAC IRMS - Artisan Console Entrypoint
 * ---------------------------------------
 * This script boots the Laravel framework and runs the command-line kernel.
 * It is tailored for the MOTAC Integrated Resource Management System (Malaysia).
 *
 * Usage:
 *   php artisan <command>
 *
 * Laravel's Artisan console provides system management, asset automation,
 * helpdesk utilities, scheduling, and more for MOTAC IRMS.
 */

define('LARAVEL_START', microtime(true));

/*
|--------------------------------------------------------------------------
| Register The Auto Loader
|--------------------------------------------------------------------------
|
| Composer provides a convenient, automatically generated class loader
| for our application. Here, we require it so that all classes,
| including MOTAC IRMS custom commands, are auto-loaded.
|
*/
require __DIR__ . '/vendor/autoload.php';

/*
|--------------------------------------------------------------------------
| Bootstrap The Application
|--------------------------------------------------------------------------
|
| This loads the Laravel application instance and prepares it for CLI usage.
| The bootstrap process attaches environment variables, config, service
| providers, and all custom behavior required by MOTAC IRMS.
|
*/
$app = require_once __DIR__ . '/bootstrap/app.php';

/*
|--------------------------------------------------------------------------
| Run The Artisan Application
|--------------------------------------------------------------------------
|
| Here, we resolve the Console Kernel and run the incoming command.
| This is the main entry point for all Artisan commands, including
| MOTAC IRMS-specific CLI operations (asset, loan, helpdesk, etc).
|
*/
$kernel = $app->make(Illuminate\Contracts\Console\Kernel::class);

$status = $kernel->handle(
    $input = new Symfony\Component\Console\Input\ArgvInput,
    new Symfony\Component\Console\Output\ConsoleOutput
);

/*
|--------------------------------------------------------------------------
| Shutdown The Application
|--------------------------------------------------------------------------
|
| Once Artisan has finished running, we fire shutdown events so that
| MOTAC IRMS cleanup or logging can occur before the process ends.
|
*/
$kernel->terminate($input, $status);

// Exit with the appropriate code for the shell/CI/CD pipeline.
exit($status);
